apply plugin: 'com.android.application'

android {

    compileSdkVersion 22
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "aifa.assignment3"
        minSdkVersion 22
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"

        compileSdkVersion 22
    }

    sourceSets {
        main {
            aidl.srcDirs = ['src/main/java']
        }
    }
}